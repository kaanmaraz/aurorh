{% extends 'base.html.twig' %}

{% block title %}TypeCandidat{% endblock %}

{% block body %}
    <h1>TypeCandidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_candidat.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_candidat.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_candidat_index') }}">back to list</a>

    <a href="{{ path('app_type_candidat_edit', {'id': type_candidat.id}) }}">edit</a>

    {{ include('type_candidat/_delete_form.html.twig') }}
{% endblock %}
